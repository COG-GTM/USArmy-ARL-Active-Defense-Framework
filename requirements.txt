# Pinned runtime dependencies for reproducible ATO builds.
# Generated from setup.py install_requires + extras_require by the
# !army_dsop_shift_left pipeline. Customer rebuilds with `pip-compile` in
# DSOP CI to produce a fully resolved lockfile.
#
# All versions below were verified against pip-audit / OSV / Grype at the
# pinned scan ref; no known CVEs at time of pinning.
#
# Core (install_requires)
IPy==1.1
dpkt==1.9.8

# Optional extras — customer opts in per deployment profile
# [mqtt]
paho-mqtt==1.6.1
# [tap]
python-pytun==2.4.1
# [pcap]
pcap-ct==1.3.0b3
# [can]
python-can==4.6.1
cantools==41.3.1
# [nfqueue]
netfilterqueue==1.1.0
